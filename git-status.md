# Definition: git status

The git status command provides a snapshot of the repository, showing which changes have been staged, which have not, and which changes are not tracked. It helps you understand the current status of the repository so you can decide what steps need to be taken.

In the JupyterLab IDE, you can launch the terminal and use the command "git status" or you can open the git extension to see changes and history of the repository.